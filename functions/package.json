{
    "name": "nextbilling-functions",
  "description": "Nextbilling POS backend: WhatsApp commerce webhooks, payment verification & order lifecycle",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "private": true,
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "test": "vitest run"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
